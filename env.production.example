# =============================================================================
# Pstral - Environment Configuration for Linux Production
# =============================================================================
# Rename this file to .env and customize the values
# =============================================================================

# -----------------------------------------------------------------------------
# Ollama Configuration
# -----------------------------------------------------------------------------
# URL where Ollama is running
# - Linux Docker with extra_hosts: http://host.docker.internal:11434
# - Linux native (Ollama on same server): http://localhost:11434
OLLAMA_BASE_URL=http://host.docker.internal:11434

# Model to use (must be pulled in Ollama first)
# Recommended models by context window size:
#   - ministral:latest (small, fast, 8K context)
#   - mistral:7b (balanced, 32K context)
#   - llama3:8b (good quality, 8K context)
#   - mixtral:8x7b (best quality, 32K context, requires more RAM)
OLLAMA_MODEL=ministral:latest

# -----------------------------------------------------------------------------
# Context Management
# -----------------------------------------------------------------------------
# Maximum messages to keep in conversation history
# Increase for models with larger context windows
MAX_HISTORY_MESSAGES=10

# Maximum characters to extract from uploaded files
MAX_FILE_CONTENT_LENGTH=3000

# -----------------------------------------------------------------------------
# Oracle Database (Optional - for SQL mode)
# -----------------------------------------------------------------------------
ORACLE_DSN=your-oracle-server:1521/XEPDB1
ORACLE_USER=your_user
ORACLE_PASSWORD=your_password

# -----------------------------------------------------------------------------
# Security Settings (MUST CHANGE IN PRODUCTION!)
# -----------------------------------------------------------------------------
# JWT Secret Key - Generate with:
#   python -c "import secrets; print(secrets.token_urlsafe(32))"
# or:
#   openssl rand -base64 32
SECRET_KEY=YOUR_SECURE_RANDOM_STRING_HERE

# Token expiration in minutes (480 = 8 hours for work day)
ACCESS_TOKEN_EXPIRE_MINUTES=480

