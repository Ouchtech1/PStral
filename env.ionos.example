# =============================================================================
# Pstral - Environment Configuration for IONOS VPS
# =============================================================================
# Target: Ubuntu 24.04, 16 Go RAM, 6 vCores, No GPU
# 
# INSTRUCTIONS:
#   1. Copy this file to .env: cp env.ionos.example .env
#   2. Generate a secure SECRET_KEY (see below)
#   3. Adjust settings as needed
#   4. Launch: docker compose -f docker-compose.ionos.yml up -d
# =============================================================================

# -----------------------------------------------------------------------------
# REQUIRED - Security Settings
# -----------------------------------------------------------------------------
# JWT Secret Key - MUST be changed in production!
# Generate with one of these commands:
#   python3 -c "import secrets; print(secrets.token_urlsafe(32))"
#   openssl rand -base64 32
SECRET_KEY=CHANGE_ME_GENERATE_A_SECURE_KEY

# Token expiration in minutes (480 = 8 hours for work day)
ACCESS_TOKEN_EXPIRE_MINUTES=480

# -----------------------------------------------------------------------------
# Ollama Configuration
# -----------------------------------------------------------------------------
# Model to use - ministral:8b is recommended for 16GB RAM VPS
# Other options: mistral:7b (similar size), ministral:3b (smaller, faster, less quality)
OLLAMA_MODEL=ministral:8b

# -----------------------------------------------------------------------------
# Context Management
# -----------------------------------------------------------------------------
# Maximum messages to keep in conversation history
# Lower = faster responses, less context
MAX_HISTORY_MESSAGES=15

# Maximum characters to extract from uploaded files
# Lower = faster processing, less context
MAX_FILE_CONTENT_LENGTH=5000

# -----------------------------------------------------------------------------
# Oracle Database (Optional - for SQL mode)
# -----------------------------------------------------------------------------
# Leave empty if not using Oracle
ORACLE_DSN=
ORACLE_USER=
ORACLE_PASSWORD=

